{
  "name": "dukazni-material-prototyp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Důkazní materiál 1930 — digitální prototyp v0.1 (headless engine + simulátor + hot-seat UI)",
  "scripts": {
    "test": "vitest run",
    "test:cestina": "node --env-file-if-exists=.env.local sim/brana-cestiny.js",
    "sim": "node sim/run.js",
    "dev": "vite",
    "lint": "eslint ."
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
